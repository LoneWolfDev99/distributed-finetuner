apiVersion: v1
kind: Pod
metadata:
  name: task-pv-pod
spec:
  volumes:
    - name: amol-amol-hf-1-ws
      persistentVolumeClaim:
        claimName: amol-amol-hf-1-ws
  nodeSelector:
    kubernetes.io/hostname: e2e-105-163
  containers:
    - name: task-pv-container
      image: nginx
      ports:
        - containerPort: 80
          name: "http-server"
      volumeMounts:
        - mountPath: "/dataset"
          name: amol-amol-hf-1-ws
